fastapi>=0.111
uvicorn[standard]>=0.29
pydantic>=2.7
pydantic-settings>=2.2
SQLAlchemy>=2.0.29
# PostgreSQL driver. Not needed for the default SQLite development database.
psycopg[binary]>=3.3.5 ; platform_system != "Emscripten"
# Schema migrations. Optional in development, where tables are created directly.
alembic>=1.13
# Outbound calls to Gemini for the in-app assistant.
httpx>=0.27
# Running the delivered model. Only needed when SCORING_ENGINE=ml; without
# them the registry reports the model engine as not ready and the rule
# engine stays in service.
lightgbm>=4.3
numpy>=1.26

# Security floors for packages the ones above pull in. Without them an old
# environment satisfies every line above while keeping versions with published
# advisories: h11 request smuggling (CVE-2025-43859) and starlette's
# multipart and Range-header denial of service (CVE-2024-47874,
# CVE-2025-54121, CVE-2025-62727). pip resolves a FastAPI that accepts them.
starlette>=0.49.1
h11>=0.16.0
certifi>=2024.7.4
