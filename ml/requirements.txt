# GUS-DEDEKTIV - dataset ureteci ve dogrulama bagimliliklari
# Python >= 3.11 (gelistirme ortami: 3.13.12)

pandas>=2.2,<4
numpy>=1.26,<3
scipy>=1.11
openpyxl>=3.1
XlsxWriter>=3.2
pyarrow>=15.0

# Model katmani (src/gus_model). Quantile basliklari, fusion ve TreeSHAP
# katkilari LightGBM'in kendi `pred_contrib` ciktisindan gelir; ayrica
# `shap` paketine ihtiyac YOKTUR.
lightgbm>=4.3

# Izotonik kalibrasyon, zorluk/sizinti probu ve referans baseline'lar
scikit-learn>=1.4

# PDF kaynak dogrulamasi icin (opsiyonel)
pypdf>=4.0
pillow>=10.0
